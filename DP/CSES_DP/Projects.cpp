#include <bits/stdc++.h>
using namespace std;

using ll = long long;

void solve () {
    cout << pow (3, 20) << '\n';
}

int main () {
    ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int tests = 1;
    // cin >> tests;

    for (int i = 0; i < tests; i += 1) {
        solve ();
    }
}